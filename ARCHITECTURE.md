# ARCHITECTURE.md — 软件架构原则

> **权威范围**：分层与依赖方向、核心边界、确定性要求、角色建模接口、事件流、性能立场、决策记录（ADR）。
> **不负责**：游戏规则与数值 → [SPEC.md](SPEC.md)；目录位置 → [PROJECT_MAP.md](PROJECT_MAP.md)；任务顺序 → [ROADMAP.md](ROADMAP.md)；测试策略 → [TESTING.md](TESTING.md)。
> **变更成本**：中。**每次修改必须追加一条 ADR 决策记录。**

---

## 1. 分层与依赖方向

系统分为三层，依赖**单向流动，不可逆**：

| 层 | 名称 | 职责 | 依赖 |
|---|---|---|---|
| **L0** | 战斗核心（sim core） | 计算战斗状态、推进时间轴、产生事件 | **无依赖** |
| **L1** | 适配层 | 加载数据、驱动一次战斗、CLI 入口 | 只能依赖 L0 |
| **L2** | 表现层 | 战斗日志、报表、未来的网页 UI | 依赖 L0（事件与数据类型）；除非确有需要，不依赖 L1 |

**没有**任何东西依赖 L2（分层之外的合成根 `src/main.ts` 除外，见第 8 节 ADR-003）。L0 不得 import L1 或 L2。

> 目录结构见第 8 节 ADR-003。

## 2. 核心边界

**L0 只做两件事**：计算战斗状态、产生事件。

**L0 明确不做**：

1. 不做任何输入输出（文件、网络、标准输出、环境变量）
2. 不做任何渲染或格式化（不生成给人类看的字符串）
3. 不读取真实时间
4. 不持有全局可变单例状态
5. 不感知"谁是 UI"

**判定标准**：如果一段逻辑在"跑一万次取分布"时不需要执行，它就不属于 L0。

## 3. 确定性要求

本节是 [AGENTS.md](AGENTS.md) 原则 4 的**操作性定义**。原则 4 的基础禁止项（`Math.random()`、`Date.now()`、真实时间、网络、IO）在此不再重复。

**扩展禁止项**——以下同样属于非确定性来源，同样禁止：

| 来源 | 说明 |
|---|---|
| **迭代顺序依赖** | `Object.keys()`、`Set`、`Map` 的遍历顺序不得影响战斗结果 |
| **浮点累加顺序** | 浮点加法不满足结合律；同一批数值以不同顺序求和可能产生不同结果 |
| **异步 / 并发** | 任何 `await`、回调、定时器都可能改变执行顺序 |
| **本地化与时区** | 不得依赖区域设置、时区、`toLocale*` |

**实现要求**：

1. **RNG 必须注入**，不得在 L0 内部全局导入或直接调用。种子是本次战斗的输入之一。
2. **同一种子 + 同一输入 → 逐字节相同的事件序列**。这是可验证的硬性要求。
3. 集合类数据结构若参与战斗决策，其遍历顺序必须显式确定。

> **未决**：时间轴相关数值（如何动值等）在核心内应以整数、定点还是浮点表示，见第 9 节。
> 该决策**依赖** [SPEC.md](SPEC.md) 中相关机制的证据结论，不得提前假定。

## 4. 角色建模：数据 + 效果 + 触发器

**核心约束（[AGENTS.md](AGENTS.md) 原则 3）**：角色由 **数据 + 效果 + 事件触发器 / hooks** 构成。

**禁止**：

- 角色名分支：`if (character === "某角色") { ... }`
- 角色专属逻辑散落在核心流程中
- 为单个角色修改核心算法

**要求**：

1. **统一的钩子集合是唯一扩展点**，例如（**示意性命名，非最终接口**）：
   `onBattleStart` / `onTurnStart` / `onBeforeHit` / `onHit` / `onKill` / …
   钩子的**实际形状必须尽早定稿**——后期推倒会波及全部角色。
2. **接口形状的确定属于架构决策**，须走 ADR。
3. 第一期只实现少量钩子，但**接口设计必须为后续机制预留位置**。

**重要边界——数据驱动不是后门**：

> 数据驱动能提供的是**结构**（schema、钩子集合），不是**语义与数值**。
> **只有状态为 `VERIFIED` 的机制才可被配置化实现。**
> "只是加了个 JSON 字段"**不构成**绕过 [AGENTS.md](AGENTS.md) 原则 1 的理由。

## 5. 事件流

- **L0 的唯一输出是有序事件序列。** 战斗过程即事件流。
- 日志、统计、报表、未来的 UI **全部是事件流的消费者**，只读不改。
- 禁止 L0 直接打印、渲染或生成面向人类的输出。
- 事件序列必须可序列化，且在同一 seed 下完全可复现。

## 6. 性能立场

- **第一期不做性能优化。** 正确性与可复现性优先。
- 但结构上必须允许"同一输入跑 N 次取分布"（批量 / 蒙特卡洛），因此不得引入跨战斗共享的可变状态。

## 7. 长期架构禁止项

以下为长期约束，不因阶段变化而放宽：

1. L0 不得依赖 L1 / L2
2. L0 不得直接产生面向人类的输出
3. L0 不得读取文件、网络、环境变量
4. L0 不得持有跨战斗的全局可变状态
5. **不得出现角色名分支**
6. 不得让测试或实现成为游戏规则的来源

> 各阶段的**范围性**不做清单由 [ROADMAP.md](ROADMAP.md) 定义，本节只定义架构性约束。

## 8. 决策记录（ADR）

**每次修改本文件必须追加一条 ADR。** 格式：

```text
### ADR-<NNN> <决策标题>

- 日期：
- 状态：提议 | 已采纳 | 已废弃
- 背景：<为什么需要做这个决策>
- 决策：<决定了什么>
- 备选方案：<考虑过但未采用的方案>
- 后果：<带来的影响与代价>
```

**ADR 记录**：

| ID | 标题 | 状态 |
|---|---|---|
| ADR-001 | Initial Architecture Baseline / 初始架构基线 | 已采纳 |
| ADR-002 | 技术栈基线（Phase 1） | 已采纳 |
| ADR-003 | Phase 1 目录结构 / Composition Root | 已采纳 |

### ADR-001 Initial Architecture Baseline / 初始架构基线

- 日期：2026-09-11
- 状态：已采纳
- 背景：Phase 0 建立项目宪法时，需要为战斗模拟器确立基础架构原则。项目目标要求战斗核心必须与 UI、CLI、数据展示完全分离，战斗结果必须可复现，且用户能够制作自定义角色。这些约束必须在编写任何代码之前固化，否则实现阶段会反复推翻。
- 决策：采纳本文件第 1~7 节所述的初始架构基线：
  1. 分三层：L0 战斗核心、L1 适配层、L2 表现层（第 1 节）
  2. 依赖单向流动；L0 无依赖；没有任何东西依赖 L2（第 1 节）
  3. L0 只做两件事——计算战斗状态、产生事件；不做 IO、渲染、真实时间、UI 感知，不持有全局可变状态（第 2 节）
  4. 确定性为硬性要求：RNG 必须注入，同 seed 逐字节可复现；禁止迭代顺序依赖、浮点累加顺序依赖、异步、本地化与时区（第 3 节）
  5. 角色建模采用「数据 + 效果 + 事件触发器」，禁止角色名分支；钩子集合是唯一扩展点；数据驱动不得成为未验证机制的后门（第 4 节）
  6. 事件流是 L0 的唯一输出，日志 / 统计 / UI 均为只读消费者（第 5 节）
  7. 第一期不做性能优化，但结构上必须支持批量运行（第 6 节）
  8. 采纳第 7 节的长期架构禁止项
- 备选方案：
  - 不分层，核心与 CLI / 日志混写 → 否决。核心将无法独立测试，且与「核心必须与 UI、CLI、数据展示完全分离」的项目目标直接冲突。
  - 核心内直接调用随机数与真实时间，测试时再打桩 → 否决。无法保证同 seed 可复现，破坏调试与回归测试的前提。
  - 角色专属逻辑以角色名分支写在核心流程中 → 否决。与「用户可制作自定义角色」的项目目标冲突，无法扩展。
  - 以「先跑起来，之后再重构」为由推迟架构决策 → 否决。核心边界与确定性约束一旦被实现违反，修复成本远高于事前确立。
- 后果：
  - 收益：核心可独立测试；结果可复现；角色可扩展；表现层可替换。
  - 代价：L0 不能直接打印调试信息，调试须通过消费事件流完成；所有随机性必须显式注入，初期实现略繁琐。
  - 约束：第 7 节的禁止项长期有效，不因阶段推进而放宽。
- 未决：本 ADR **不涉及**下列事项，它们仍为待决（见第 9 节）：时间轴数值表示方式（A-001）、事件流形态与事件粒度（A-002）、钩子接口的具体形状（A-003）。目录结构划分（A-004）已由 ADR-003 解决。

### ADR-002 技术栈基线（Phase 1）

- 日期：2026-09-11
- 状态：已采纳
- 背景：P1-01 需要确定编程语言、运行时、包管理器、测试框架与工程工具。约束来自项目目标：确定性战斗核心 + CLI；测试（尤其 golden 对平）为核心资产；尽量少依赖、低工程复杂度；AI 协作者需要易于理解与维护的环境；Phase 1 不做 UI。
- 决策：
  1. **语言**：TypeScript（strict 全开，类型即文档）
  2. **运行时**：Node.js，最低版本 **>= 24.18.0**。依据：本项目依赖 Node 原生 TypeScript type stripping；如需放宽版本要求，必须实际验证后修改本 ADR
  3. **包管理器**：npm
  4. **测试框架**：Node 内置 `node:test` + `node:assert`；Phase 1 不引入第三方测试框架
  5. **类型检查与构建**：`tsc --noEmit` 做类型检查；Phase 1 不使用构建工具，`node` 直接运行 `.ts`
  6. **依赖**：运行时依赖 **0**；开发依赖仅 `typescript`、`@types/node`
  7. **TypeScript 配置**：`strict: true`、`noUncheckedIndexedAccess: true`、`module: "nodenext"`、`moduleResolution: "nodenext"`、`erasableSyntaxOnly: true`、`verbatimModuleSyntax: true`、`allowImportingTsExtensions: true`、`noEmit: true`；不含 DOM lib；单一 `tsconfig.json`
  8. **语法纪律**：不使用 enum / namespace / 参数属性等需要运行时转换的 TypeScript 语法（由 `erasableSyntaxOnly` 强制）
  9. **相对 import**：当前直接运行 `.ts` 的模式下使用 `.ts` 扩展名。`rewriteRelativeImportExtensions` 可以保留，但必须明确：Node 原生运行 `.ts` 不依赖该选项；Node 的 type stripping 不读取 `tsconfig.json`；该配置主要服务 TypeScript 类型检查以及未来可能的 emit 路线
  10. **ESLint / Prettier**：Phase 1 **不引入**。这是 Phase 1 的范围决定，不是永久架构禁止项；代码规模增长后可重新评估
- 备选方案：
  - JavaScript → 否决。无类型保护，错误推迟到运行时 / golden 对平才暴露，与正确性优先目标冲突。
  - Bun / Deno → 否决。生态与长期稳定性不如 Node，本项目无性能或一体化需求。
  - pnpm / yarn → 否决。依赖极少，无收益且引入额外概念。
  - Vitest / Jest → 否决。golden 对平是数据驱动的确定性比对，内置 `node:test` 足够；第三方框架增加依赖与配置面。
  - 构建工具（tsup / esbuild / rollup）与 tsx → 暂缓。发布分发或遇到类型擦除边界时再评估（tsx 为最小回退，届时另立 ADR）。
- 后果：
  - 收益：工程最小化；官方工具链稳定性高；AI 协作面小；测试一条命令可跑；Phase 1 零运行时依赖。
  - 代价：语法受 `erasableSyntaxOnly` 约束；要求 Node >= 24.18.0；`node:test` 特性少于第三方框架（Phase 1 不需要）。
  - 约束：Phase 1 运行时依赖为 0；任何新增依赖须走 [ROADMAP.md](ROADMAP.md) D-003 审批流程。
- 未决：本 ADR 不涉及时间轴数值表示（A-001）、事件流形态（A-002）、钩子接口（A-003），均保持待决。目录结构（A-004）已由 ADR-003 解决。

### ADR-003 Phase 1 目录结构 / Composition Root

- 日期：2026-09-11
- 状态：已采纳
- 背景：A-004 目录结构划分阻塞 P1-02（工程初始化）。目录结构必须体现第 1 节的 L0 / L1 / L2 分层，覆盖 Phase 1 需要：战斗核心、CLI、单元 / 集成 / golden 测试、golden 样本数据；且不为未来机制提前建设目录。
- 决策：
  1. 采纳 Phase 1 目标结构（本 ADR 仅为文档决策，不创建目录）：
     ```text
     src/
     ├── core/       # L0 战斗核心
     ├── cli/        # L1 适配层：CLI 入口 + 场景数据加载
     ├── report/     # L2 表现层：事件流 → 战斗日志渲染
     └── main.ts     # composition root，不属于 L0 / L1 / L2
     tests/
     ├── unit/       # 单元测试
     ├── integration/# 集成测试
     └── golden/     # golden 测试代码
     data/
     └── golden/     # golden 样本数据（输入场景 + 期望输出 + 来源记录）
     ```
  2. 依赖规则：
     - `src/core/` → 不依赖其他产品层
     - `src/cli/` → 只依赖 `core/`
     - `src/report/` → 原则上只依赖 `core/` 中的事件 / 数据类型；不要为了理论允许而主动依赖 L1
     - `src/main.ts` → 唯一 composition root，可以组装 `core / cli / report`；**只负责应用组装和启动**，不包含游戏规则、战斗逻辑或日志渲染逻辑
  3. `src/core/` 从**扁平模块结构**起步：Phase 1 不创建 `core/` 子目录；仅在真实复杂度出现后，再通过架构决策按职责拆分；不得为了未来机制提前建目录。此条为 Phase 1 决策，**不是**长久架构禁止项。
  4. 现在明确不创建：`src/characters/`、`src/effects/`、`src/buffs/`、`src/skills/` 等机制目录；`src/web/`、`frontend/` 等 UI 目录；`dist/`、`build/`、`coverage/`；`benchmarks/`、`scripts/`、`docs/`；`tests/fixtures/`。
  5. golden 样本数据的存放位置由 [PROJECT_MAP.md](PROJECT_MAP.md) 记录（[TESTING.md](TESTING.md) 第 3 节已指向其）。样本内部格式属 P1-08 后续决策，本 ADR 不决定。
  6. **关闭 A-004**：经复核，目录结构不依赖 A-001 / A-002 / A-003 的结论——无论三者如何裁决，本结构不受影响；原依赖记录修正为不阻塞。
- 备选方案：
  - composition root 放在 `src/cli/main.ts` 并声明其为 L1 的特殊例外 → 否决。给 L1 设例外会模糊层级边界，独立 `src/main.ts` 更清晰。
  - `src/core/` 预建按机制拆分的子目录 → 否决。Phase 1 没有该复杂度，违反"不过度设计"。
  - L2 渲染逻辑并入 `src/cli/` → 否决。混层，无法用目录结构表达"没有产品层依赖 L2"。
  - 样本数据并入 `tests/golden/` → 否决。样本是带来源记录的证据数据，与测试代码分离更清晰。
- 后果：
  - 收益：层级依赖可由目录结构直接检查；L0 保持扁平；合成根唯一且不含业务逻辑。
  - 代价：多出 `src/report/` 目录与 `src/main.ts` 文件。
  - 约束：`src/main.ts` 若开始承载游戏规则、战斗逻辑或日志渲染逻辑，即违反本 ADR，须另立 ADR 修正。
- 未决：A-001、A-002、A-003 仍保持待决（见第 9 节）。

## 9. 待决架构问题

| # | 问题 | 阻塞对象 | 依赖 |
|---|---|---|---|
| A-001 | 时间轴相关数值的表示方式（整数 / 定点 / 浮点） | 时间轴调度的实现 | [SPEC.md](SPEC.md) 中相关机制的证据结论 |
| A-002 | 事件流的形态与事件粒度 | 核心输出接口 | — |
| A-003 | 钩子接口的具体形状 | 角色建模 | — |
| A-004 | 目录结构划分（**已解决**，见 ADR-003） | 工程初始化 | 原记录 A-001 ~ A-003，经复核不阻塞 |
