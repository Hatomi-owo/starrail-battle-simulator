# AGENTS.md — AI 协作规则

> **地位**：项目宪法。约束所有 AI 协作者与人类贡献者在本仓库中的行为。
> **权威范围**：工作方式、行为红线、原则优先级、任务粒度、汇报格式。
> **不负责**：游戏规则 → [SPEC.md](SPEC.md)；代码结构 → [ARCHITECTURE.md](ARCHITECTURE.md)；文档职责边界与治理 → [PROJECT_MAP.md](PROJECT_MAP.md)；任务定义 → [ROADMAP.md](ROADMAP.md)；测试策略 → [TESTING.md](TESTING.md)。
> **变更成本**：修改本文件需**项目负责人确认**。

---

## 1. 强制阅读顺序

每次开始工作前，按顺序读完以下文档，再动手：

1. [PROJECT_MAP.md](PROJECT_MAP.md) — 东西在哪，哪份文档说了算
2. [SPEC.md](SPEC.md) — 游戏规则的现状与未知项
3. [ARCHITECTURE.md](ARCHITECTURE.md) — 代码如何组织
4. [ROADMAP.md](ROADMAP.md) — 当前阶段与任务
5. [TESTING.md](TESTING.md) — 如何证明做对了
6. 本文件

未读完不得开始编码。不得以"任务很小"为由跳过。

## 2. 项目宪法

以下十条是本项目的最高行为准则。

| # | 原则 |
|---|---|
| 1 | **不凭记忆实现游戏机制。** 任何游戏数值、倍率、公式、行动值规则、抗性、伤害计算等，必须先进入 [SPEC.md](SPEC.md) 并有明确来源。无可靠证据者标记为 `UNKNOWN`，禁止实现。**只有 `VERIFIED` 的机制允许进入正式模拟器核心**；各状态的允许用途见 [SPEC.md](SPEC.md) 第 2 节。 |
| 2 | **代码不能反过来决定游戏规则。** 正确顺序：游戏规则 → SPEC.md → 测试 → 实现。禁止"先写代码，再改 SPEC 让代码看起来正确"。 |
| 3 | **角色不得硬编码进核心。** 禁止 `if (character === "某角色")` 之类的分支。角色由 **数据 + 效果 + 事件触发器 / hooks** 构成。操作性定义见 [ARCHITECTURE.md](ARCHITECTURE.md)。 |
| 4 | **战斗核心必须可复现。** 禁止 `Math.random()`、`Date.now()`、真实时间、网络请求、外部 IO。使用 seeded RNG，保证 **相同输入 + 相同 seed = 相同战斗结果**。扩展禁止项与实现要求见 [ARCHITECTURE.md](ARCHITECTURE.md)。 |
| 5 | **UI 与战斗核心完全分离。** 核心只负责计算战斗状态、产生事件。CLI、日志、未来网页 UI 都只是消费者。 |
| 6 | **测试是护栏。** 任何机制先定义 Given / When / Then。测试失败时先判断是代码错还是规范错。禁止为了让测试通过而随意修改 SPEC 或测试期望。详见 [TESTING.md](TESTING.md)。 |
| 7 | **不得擅自扩大任务范围。** 不顺手重构、不改命名、不升级依赖、不做 UI、不实现 ROADMAP 后面的功能。一次只完成当前任务。 |
| 8 | **遇到不确定必须停下。** 规则不明确、来源互相矛盾、SPEC 与代码冲突、架构可能需要重大改变时，不得自行猜测。先报告问题并给出候选方案。 |
| 9 | **Git 操作受控。** 未经项目负责人明确要求，不得 commit、push 或修改 Git 历史。 |
| 10 | **每次工作前先读文档。** 见第 1 节。 |

## 3. 原则优先级

原则之间冲突时，按此顺序裁决（序号靠前者优先）：

1. **正确性**（原则 1、2）
2. **停下来报告**（原则 8）
3. **范围控制**（原则 7）
4. 效率、代码整洁、顺手优化

**明确裁定：原则 8 高于原则 7。** 发现问题后停下报告，**不属于**"扩大范围"；发现问题后仍继续实现，才属于违规。

## 4. 任务粒度

- 每个开发任务必须对应 [ROADMAP.md](ROADMAP.md) 中一个明确的 **Task ID**。无 Task ID 的工作不得开始。
- 任务粒度由 ROADMAP 定义，**不由执行者自行判断**。
- 一个任务完成后立即停止并汇报，**不得自动接续下一个任务**。
- 任务范围以 Task ID 条目描述为准。任务描述未提及的文件，默认不在范围内。

## 5. 行为红线

以下行为一律禁止：

1. 实现或正式测试 [SPEC.md](SPEC.md) 中状态**非 `VERIFIED`** 的机制（含 `DOCUMENTED`、`DISPUTED`、`UNKNOWN`）
2. 凭记忆编造任何游戏数值、倍率、公式、角色数据
3. 为了让测试通过而修改 SPEC、测试期望值或测试用例
4. 在战斗核心中引入非确定性（含 [ARCHITECTURE.md](ARCHITECTURE.md) 列出的扩展禁止项）
5. 未经请求的重构、改名、依赖变更、结构改动
6. 自行 commit / push / 改写 Git 历史
7. 修改 `README.md` 与 `.gitignore`，除非任务明确要求
8. 把猜测写成事实——包括在代码注释、文档与汇报中
9. 用"示例/占位"的内容冒充项目事实；举例必须显式标注

## 6. 不确定时的处理

发现问题时**必须停止当前实现**，并完成两件事（缺一不可）：

1. **在对话中报告**：说明问题、影响范围、候选方案与你的建议
2. **落盘记录**：
   - 机制类不确定 → 记入 [SPEC.md](SPEC.md)，按证据情况记为 `UNKNOWN` / `DOCUMENTED` / `DISPUTED`，或登记为开放问题：
     - `UNKNOWN`：尚无可靠证据
     - `DOCUMENTED`：已找到部分 / 二手证据，但**尚未达到 `VERIFIED` 标准**
     - `DISPUTED`：来源互相矛盾，尚未解决
     - 开放问题：问题尚未定形，或不属于单条机制
   - 状态定义与各状态的允许用途以 [SPEC.md](SPEC.md) 第 2 节为**唯一权威**；本节仅作完整性补充，不重复定义。
   - 注意：`DOCUMENTED` **不豁免 `VERIFIED` 门槛**——不得进入 sim core、不得进入正式测试、不得成为产品行为的一部分。
   - 架构类不确定 → 记入 [ARCHITECTURE.md](ARCHITECTURE.md) 的决策记录

只报告不落盘视为未完成——会话结束后该问题会丢失，下一个协作者会重新踩一遍。

## 7. 汇报格式

任务完成后，汇报必须包含：

1. 改动了哪些文件
2. 每个改动的目的
3. **是否修改了任务范围外的文件**（如无，明确写"无"）
4. 是否新增依赖
5. 是否执行 Git 操作
6. 测试结果——**失败必须如实报告并附输出**，不得省略或美化
7. 遗留问题与新发现的 `UNKNOWN`

## 8. 相关文档

- 文档职责边界、SSOT 与文档治理规则 → [PROJECT_MAP.md](PROJECT_MAP.md)
- 游戏规则与证据状态 → [SPEC.md](SPEC.md)
- 分层、确定性、角色建模 → [ARCHITECTURE.md](ARCHITECTURE.md)
- 测试规范 → [TESTING.md](TESTING.md)
- 任务与阶段 → [ROADMAP.md](ROADMAP.md)
