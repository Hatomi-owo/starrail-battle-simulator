# TESTING.md — 测试规范

> **权威范围**：测试分层、样本规范、Given/When/Then 要求、失败处理流程、覆盖目标、完成标准。
> **不负责**：游戏规则与期望值本身 → [SPEC.md](SPEC.md)；代码结构 → [ARCHITECTURE.md](ARCHITECTURE.md)；任务定义 → [ROADMAP.md](ROADMAP.md)。
> **变更成本**：中。修改需说明理由。

---

## 1. 测试的目标

> **真正的判据不是"测试通过"，而是"期望值能追溯到证据"。**

**测试通过只证明「实现与测试一致」，不证明「机制正确」。**
一个错误的 SPEC 会产生错误的测试，进而让错误的实现"全绿通过"。因此：

- 期望值**必须**能追溯到 [SPEC.md](SPEC.md) 中的条目及其来源。
- 无法追溯来源的期望值，**视同无效测试**。
- 测试**不是**游戏规则的来源，不得用它反推规则（见 [AGENTS.md](AGENTS.md) 原则 2）。

## 2. 测试分层

| 层 | 对象 | 作用 |
|---|---|---|
| **单元** | 单个公式 / 单个函数 | 验证计算的局部正确性 |
| **集成** | 一整场战斗 | 验证事件序列、行动顺序、状态流转 |
| **Golden** | 对照真实证据的完整场景 | **最高权威**，锚定"算得对" |

**Golden 层是本项目最重要的测试层**，因为模拟器的全部价值在于结果正确。

## 3. Golden 样本规范

每个 golden 样本必须包含：

| 字段 | 要求 |
|---|---|
| 输入场景 | 完整描述：参与的双方、初始状态、执行的动作序列 |
| 期望输出 | 行动顺序、伤害数值、事件序列 |
| **来源** | 等级 + 具体出处 + 采集方式 + 采集日期 |
| `game_version` | 样本声明自己的**目标版本**。项目不设全局锁定版本（见 [SPEC.md](SPEC.md) 第 4 节）；样本版本与所涉条目 `last_verified_version` 的差异**保持可见**，不默认"无变化" |

**没有可追溯来源的样本，不得进入测试。**
样本来源形式须遵循 [SPEC.md](SPEC.md) 第 3 节的来源分级。

> 样本数据的具体存放位置：待工程初始化后由 [PROJECT_MAP.md](PROJECT_MAP.md) 记录。

## 4. Given / When / Then

任何机制在实现前必须先定义测试：

- **Given**：初始状态（参与者、属性、场上状态）
- **When**：触发的动作或事件
- **Then**：期望结果（数值、状态变化、产生的事件）

**Given / When / Then 必须先于实现写出**（[AGENTS.md](AGENTS.md) 原则 2、6）。

## 5. 期望值与证据的绑定

- 每条测试必须引用它所属的 **SPEC 条目 ID**，使失败能直接定位到机制。
- 期望值不得来自：
  - AI 记忆
  - 现有实现的输出（自证循环）
  - 其他测试的输出
- 若某机制尚无 `VERIFIED` 条目，则**不能**为它写正式测试（见第 7 节）。

## 6. 测试失败的处理流程

测试失败时，**先停止，不要修改任何东西**，按以下顺序判断：

1. **判断错在哪一方**：代码错 / 测试·样本错 / SPEC 错 / **版本机制发生变化** / 无法判定
2. **若代码错** → 修代码
3. **若测试错或 SPEC 错** → **必须先找到证据依据**，按变更成本要求更新 [SPEC.md](SPEC.md)（附证据变化说明），**然后**才更新测试
4. **若疑似版本机制发生变化** → 对平失败只是触发版本复查的**信号之一**，不是版本变化的直接证据；确认版本变化必须依据 [SPEC.md](SPEC.md) 第 3 节的证据规则
5. **若无法判定，或发现来源矛盾** → **停止并报告**（[AGENTS.md](AGENTS.md) 原则 8）

**禁止**：直接修改期望值让测试变绿。
**禁止**：为了让测试通过而放宽 SPEC。

## 7. 禁止事项

1. **禁止为状态非 `VERIFIED` 的机制编写正式测试**（含 `DOCUMENTED`、`DISPUTED`、`UNKNOWN`）。
   这会把猜测固化成"事实"，且是静默的——测试是绿的，没人会去质疑它。
   `DOCUMENTED` 允许**独立研究性实验**，但该实验不得进入正式测试套件，且必须明确标注为非规范内容。
2. 禁止修改期望值以迁就实现。
3. 禁止依赖真实时间、网络、系统随机、执行顺序、时区。
4. 禁止编写无来源的期望值。
5. 禁止把测试结果当作游戏规则的依据。
6. 禁止为了让测试可跑而跳过或禁用测试而不报告。

## 8. 覆盖目标

**覆盖率不是本项目的目标。** 替代指标：

> **每条状态为 `VERIFIED` 的 SPEC 条目，至少被一个测试锚定。**

- 该指标可核查：SPEC 条目数 vs 被引用的条目 ID 数。
- 未达标的 `VERIFIED` 条目视为"规范已确认但未受保护"。

## 9. 边界与反例

以下类型必须覆盖（具体取值在 SPEC 条目中确定）：

- 边界值：零值、上限、下限
- 平局：两个参与者的关键排序值相同
- 同时发生：同一时刻的多个事件
- 空集合：无行动者、无事件
- 顺序依赖：结果是否受处理顺序影响

## 10. 完成标准

阶段性的完成标准由 [ROADMAP.md](ROADMAP.md) 定义。本节只定义**测试层面**的通用要求：

- 所有 `VERIFIED` 条目均被锚定
- 同一 seed 重复运行结果完全一致
- 无被跳过的测试（如有，必须在汇报中列出并说明）
