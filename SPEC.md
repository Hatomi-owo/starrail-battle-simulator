# SPEC.md — 游戏规则规范

> **地位**：本项目关于游戏规则的**唯一规范来源**。
> **权威范围**：机制、公式、数值、行动值规则、抗性、伤害计算、证据来源、证据状态、`game_version`。
> **不负责**：代码如何组织 → [ARCHITECTURE.md](ARCHITECTURE.md)；如何测试 → [TESTING.md](TESTING.md)；先做什么 → [ROADMAP.md](ROADMAP.md)。
> **变更成本**：**高**。任何修改必须说明**证据变化**。

---

## 1. 本文件的地位

**SPEC 是项目内部关于游戏规则的唯一规范来源；SPEC 中的事实必须有可追溯证据。**

- 不同证据发生冲突时，**不得由 AI 记忆、代码或测试结果自行裁决**。必须重新调查并更新 SPEC。
- 代码、测试、文档中的任何游戏规则陈述，若与 SPEC 冲突，以 SPEC 为准（但 SPEC 本身可被新证据推翻）。
- **未在本文件中登记的游戏规则，在本项目内视为"未规范化"，不得作为正式实现、正式测试或产品行为的依据。**
- 顺序不可颠倒：**SPEC → 测试 → 实现**。禁止由实现反推 SPEC（见 [AGENTS.md](AGENTS.md) 原则 2）。
- **本文件当前为空。** 在完成证据调查前，不登记任何机制条目。

## 2. 证据状态

每条机制条目必须且只能标注以下状态之一：

| 状态 | 含义 | 允许的用途 |
|---|---|---|
| `VERIFIED` | 已用可信来源核对，并记录了来源与核对方（合格证据的判定见第 3 节） | **允许正式实现、正式测试**。唯一可进入正式模拟器核心的状态 |
| `DOCUMENTED` | 有二手来源，尚未亲自验证 | 仅允许**调查、记录、分析、独立研究性实验**；**不得进入 sim core，不得进入正式测试，不得成为产品行为的一部分** |
| `DISPUTED` | 多个来源互相矛盾，尚未解决 | 仅允许**调查**；**禁止正式实现、禁止正式测试** |
| `UNKNOWN` | 尚未确立 | 仅允许**调查**；**禁止正式实现、禁止正式测试** |

**目标原则：只有 `VERIFIED` 的机制允许进入正式模拟器核心。**

**裁定：非 `VERIFIED` 不是"以后再补"，而是"不许写进正式实现"。**
禁止范围包含：正式实现、正式测试、以及在文档或注释中将其表述为已知事实。
`DOCUMENTED` 所允许的**独立研究性实验**必须与产品代码、正式测试套件隔离，且不得成为产品行为的一部分。
依据见 [AGENTS.md](AGENTS.md) 第 5 节；测试侧的执行规则见 [TESTING.md](TESTING.md)。

## 3. 证据来源分级

本节定义**证据资格**：各等级来源在什么条件下能支撑 `VERIFIED`。
`VERIFIED` 的唯一判据是：**存在足以直接支持该具体声明的合格证据**，而不是机械凑齐固定证据组合。
证据等级定义"证据资格"；具体声明是否 `VERIFIED`，取决于证据是否真正覆盖该声明。

| 等级 | 来源类型 | 资格 |
|---|---|---|
| **A** | 项目内可复现的真机实测 | 对实验**实际覆盖到**的行为、时序、结果，可单独支持 `VERIFIED`；必须记录采集方法、条件与证据对应的 `game_version`。未覆盖的范围不得顺带 `VERIFIED`，写入条目「开放问题」 |
| **B** | 数据挖掘 / 游戏数据文件 | 对数据中**直接呈现、可直接读取**的事实，可单独支持 `VERIFIED`；必须记录版本与提取方式 |
| **C** | 社区 wiki / 资料站 | 单个至多 `DOCUMENTED`；两个**真正独立**（不互相引用、不同源）且对**同一具体声明**一致的 C，可支持 `VERIFIED` |
| **D** | 普通玩家演示 / 攻略 / 观点 | **单独或任何组合都不能**升级到 `VERIFIED`；用途限于调查线索、旁证、触发复查 |

**定级原则——按证据性质，不按承载媒介**：

- 等级以声明**最原始、可追溯证据的性质**定级，与网页、视频、wiki 等承载媒介无关。
- 视频记录了完整、可复现的真机实验 → 视频本身是调查材料（D）；**项目按其条件完成复现后，项目自己的复现实验 = A**。
- wiki 引用可追溯的数据文件 → 该声明按 B 定级。
- 两个网站转述同一出处 → 算**一个**来源。

**推理链要求**：

- 若结论需要在 A / B 原始证据之上做**额外推理**（从数据字段推断语义、从实测数值反推公式结构、把数值归属到某技能等），**推理链本身必须得到证据支持**；原始材料等级高，不自动使推导结论成为 `VERIFIED`。
- 推理链无证据支持的部分不标 `VERIFIED`：记为该条目「开放问题」，或按 `DOCUMENTED` 处理。

**冲突裁决**：

- 按"哪个证据**更直接支持当前具体声明**"判断，不机械按 A > B 排序。
- 同版本、无法解释的冲突 → `DISPUTED` + 复查（版本、采集条件、字段归属）；仍无法解释 → 按 [AGENTS.md](AGENTS.md) 原则 8 停下报告。
- 版本不同、各自在其版本下有证据 → 按版本机制拆条目（见第 4 节）。

**测试结果不是证据**：

- golden 样本背后的**真实来源**（如项目内真机实测）才是证据。例（示意，非项目事实）：某版本真机实测 → 采集可追溯 golden 样本 → 实测本身形成 A 级证据 → 若直接覆盖相应声明，可据此把该条目的 `last_verified_version` 更新到该版本。
- 此后模拟器通过该 golden，**只证明实现与该证据一致**，不产生新的游戏规则证据；**不得**因对平 PASS 更新任何 SPEC 条目的证据状态或版本字段。
- golden 测试**失败**是触发调查的信号，同样不是游戏机制发生变化的直接证据。

- 来源记录必须包含：等级、具体出处、采集方式、采集日期、证据对应的 `game_version`。
- 来源不可追溯的条目**不得**标记为 `VERIFIED`。

## 4. 机制条目模板

每条机制占独立一节，格式如下：

```text
### MECH-<领域>-<NNN> <机制名称>

- 状态：UNKNOWN | DOCUMENTED | DISPUTED | VERIFIED
- introduced_version：<可选。仅当有证据明确证明该规则形式自该版本起存在 / 发生变化时填写；首次观察版本 ≠ 引入版本>
- last_verified_version：<VERIFIED 条目必填。最近一次被证据确认的版本 = 证据新鲜度；不表示有效期，不产生默认区间>
- 来源：<等级 + 具体出处 + 采集方式 + 采集日期 + 证据对应的 game_version>
- 规则/公式：<必须写成可测试的「输入 → 输出」形式，不写散文>
- 边界与反例：<0 值、上限、平局、同时发生、顺序依赖等>
- 开放问题：<尚未确定的部分>
- 相关条目：<其他 MECH ID>
```

**条目规则**：

1. 公式必须**可测**：给定确定的输入，能得到确定的输出。
2. 机制随版本变动时，**新增条目，不原地改写**——保留历史，避免静默覆盖。旧条目注记变化事实；确切变更版本未知时如实记录，不得写"自版本 X 起"。
3. 版本字段规则：
   - `introduced_version`：仅当有证据明确证明该规则形式自该版本起存在 / 发生变化时填写；**首次观察版本 ≠ 引入版本**，无证据不填。
   - `last_verified_version`：`VERIFIED` 条目必填，表示最近一次被证据确认的版本（**证据新鲜度**）；不表示有效期，不产生默认有效区间。
   - **只有证据明确支持某个版本范围时**（如跨版本数据对比无变化），才可在来源或规则中声明该范围；无证据不声明。
   - **测试通过不是证据**：更新 `last_verified_version` 只能依据样本背后的真实来源（见第 3 节），不得依据对平 PASS。
4. 任何数值、倍率、常数都必须写在条目内并附来源；**不得只存在于代码、注释或测试中**。
5. 条目 ID 一经分配不得复用。
6. 状态变更必须记录变更理由与新的证据。

## 5. 机制登记表

> **当前为空。**
> 未完成证据调查的机制不得在此登记。

| ID | 机制 | 状态 | introduced_version | last_verified_version | 来源 |
|---|---|---|---|---|---|
| （暂无） | | | | | |

## 6. 待调查领域

> **本节内容为示例性占位，非项目事实。**
> 以下名称来自**项目目标**（见 [ROADMAP.md](ROADMAP.md)），仅用于组织未来的调查工作。
> 它们**不代表**其规则已知、已确认、或已被本文件认可存在。任何一条在被登记为正式条目之前，
> 状态一律视为 `UNKNOWN`。

- 速度 / 行动值
- 伤害计算
- Buff / Debuff
- 击破
- 能量
- 终结技
- 追击
- 治疗
- 护盾

## 7. 开放问题登记

规则类未决问题记录于此。项目决策类未决问题记录在 [ROADMAP.md](ROADMAP.md)。

| ID | 问题 | 影响 | 状态 |
|---|---|---|---|
| Q-001 | 证据来源的最低可接受等级（A~D 中的哪一级） | 决定多少条目有资格升为 `VERIFIED` | **已解决**（P0-02）——各等级资格与 `VERIFIED` 判据见第 3 节 |
| Q-002 | 目标 `game_version` 锁定值 | 机制会随版本漂移，不锁版本的规范会静默过期 | **已解决**（P0-02）——不设全局锁定，采用机制级版本追踪，见第 4 节 |
| Q-003 | 某项领域（如养成系统类内容）是永久排除还是推迟 | 决定调查工作量与范围 | **已解决**（P0-03）——Phase 1 不纳入养成系统类内容；"永久排除还是推迟"留到 Phase 1 复盘（P1-09）评估。范围决策见 [ROADMAP.md](ROADMAP.md) 第 4、6 节 |
